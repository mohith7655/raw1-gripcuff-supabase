// Re-export from the Firestore-backed context so every existing import keeps working.
export { useFavorites } from '../providers/FavoritesContext';
export type { FavoriteVideo } from '../providers/FavoritesContext';
