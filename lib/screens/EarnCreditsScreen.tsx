import React from 'react';
import { CreditsScreen } from './CreditsScreen';

export const EarnCreditsScreen = () => {
    // For now, mapping the "Earn Credits" route directly to the "Credits" screen.
    // In the future this can be expanded into a separate screen if needed.
    return <CreditsScreen />;
};
